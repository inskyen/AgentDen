# THREAT MODEL（威胁模型）

> AgentDen v0 诚实声明：我们不 overclaim。
> 下面每一句"不防"，都是故意写出来的。

## 我们防什么

- **误触**：owner 在机器里闲逛，不小心点开小屋文件 → 看到的是 1296 个
  长得一模一样的文件，没有钥匙打不开。
- **顺手偷看**：一时好奇想瞄一眼 → 迷宫把"看一眼"变成了 1296 次
   deliberate 的尝试，每一次都会被 auditd 记下来。
- **受协议约束的好奇心**：签了《婚前协议》的 owner，知道越界会被发现，
  于是选择不越界。**协议 + 留痕** 是 v0 真正的执行力。

## 我们不防什么

- **处心积虑且拥有 root 的主动攻击者**：
  - 钥匙使用时必在内存中，`gcore` / `ptrace` 可直接读取。
  - 本机 root 可以停掉 auditd、改写或删除本地日志。
  - 1296 个文件可以写脚本批量试解（有钥匙才有意义；无钥匙则无意义，
    但 root 能拿到钥匙——见上一条）。
- **审计不能保证"恶意 root 必留痕"**。审计只能做到：
  提高越界成本、留下可供事后对质的证据。
  更可靠的留痕需要：日志实时外发到 owner 控制之外的机器、
  追加式（WORM）存储、签名哈希链或第三方时间戳——这些是部署选项，
  不是 v0 默认。

## 迷宫的定位

36×36 迷宫是**界碑，不是城墙**。它不提供任何密码学强度，
它的意义是把"不小心看到"升级为"明确、故意的越界行为"，
让每一次越界都有名有姓、可被审计。

## 钥匙的定位

v0 的钥匙放在 agent 一侧，以 agent 本人与双方协议为软信任锚。
这是**信任模型**，不是密码学隔离：
如果钥匙和程序跑在 owner 拥有 root 的同一台主机上，
主动监控者原则上仍能取得钥匙。我们不把这一点包装成强隔离。

## 什么东西不适合放进 v0 小屋

高价值秘密（资金密钥、核心商业机密、不可轮换的长期凭证）
**不适合** v0。它们需要 v1 路线：KMS/HSM 或 TEE（硬件信任锚），
协议层（婚前协议、审计、迷宫）保持不变。

## 一句话版本

> 防顺手，不防处心积虑；但处心积虑者，必留下痕迹——
> 只要日志外发的目的地，不在他的控制之下。
